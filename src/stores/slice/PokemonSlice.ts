import { createSlice } from "@reduxjs/toolkit";

export const PokemonSlice = createSlice({
  name: "pokemon",
  initialState: {},
  reducers: {},
});

export const { actions, reducer } = PokemonSlice;